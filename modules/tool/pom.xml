<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>tech.kayys.andalus</groupId>
        <artifactId>andalus-parent</artifactId>
        <version>0.0.1</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <groupId>tech.kayys.andalus</groupId>
    <artifactId>andalus-tool-parent</artifactId>
    <packaging>pom</packaging>
        <!--
        All modules below depend on packages from the removed andalus-tool-execution artifact
        (tool.entity, tool.dto, tool.repository, tool.service, tools.spi).
        They need SPI realignment before they can build.
        <module>andalus-tool-registry</module>
        <module>andalus-tool-mcp</module>
        <module>andalus-tool-nono</module>
        <module>andalus-sandbox-docker</module>
        <module>andalus-tool-execution</module>
        <module>andalus-tool-parser</module>
        <module>andalus-tool-utcp</module>
        <module>andalus-tool-http</module>
        <module>andalus-tool-os</module>
        -->
    <modules>
        <!--
        <module>andalus-image-tools</module>
        <module>andalus-audio-tools</module>
        <module>andalus-albiruni-tool</module>
        -->
    </modules>

    <properties>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.32.2</quarkus.platform.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>


</project>
