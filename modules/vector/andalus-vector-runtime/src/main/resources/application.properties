# Vector Store Configuration
# Default vector store implementation
andalus.vector.store.type=in-memory

# Configuration for PGVector store (when enabled)
# andalus.vector.store.type=pgvector
# quarkus.datasource.db-kind=postgresql
# quarkus.datasource.username=user
# quarkus.datasource.password=password
# quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/andalus

# Configuration for other stores would go here